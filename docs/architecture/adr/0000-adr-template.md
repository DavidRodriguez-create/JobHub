# ADR 0000: <Short decision title>

<!--
Architecture Decision Record. Copy this file to `NNNN-kebab-title.md` (next number, zero-padded)
and fill it in. One ADR = one decision. Keep it short; link out for detail.
Authored by the `jobhub-architect` agent, but anyone may write one.
-->

- **Status:** Proposed <!-- Proposed | Accepted | Superseded by ADR-NNNN | Deprecated -->
- **Date:** YYYY-MM-DD
- **Deciders:** <who signed off>
- **Affects:** <services / modules, e.g. application-service, api-contracts, db/init>

## Context

<!-- The forces at play: the problem, the requirement, the constraint that prompted this.
State the relevant JobHub conventions in scope (e.g. Hexagonal vs Clean, contract-first
api-contracts, schema-per-service, DB owned by db/init SQL). Keep it factual, not a pitch. -->

<What problem are we solving and what constraints bound the solution?>

## Decision

<!-- The decision, stated as a present-tense directive: "We will ...".
Be specific enough that an implementer can follow it: which architecture (Hex/Clean),
which module, which ports/contracts/schema changes, which conventions apply. -->

We will <the decision>.

## Consequences

<!-- Honest trade-offs. What gets easier, what gets harder, what we now must maintain. -->

- Positive: <benefit>
- Negative / cost: <trade-off or new obligation>
- Follow-ups: <migrations, tests, docs, or future ADRs this implies>

## Alternatives considered

<!-- Briefly, the options not taken and why. This is what makes an ADR worth keeping. -->

- **<Alternative>** — rejected because <reason>.
- **<Alternative>** — rejected because <reason>.
