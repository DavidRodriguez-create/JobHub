# API reference — auth-service

Rendered live from `api-contracts/src/main/resources/openapi/auth-service.yaml`. Update the contract
and this page follows automatically.

<swagger-ui src="auth-service.yaml"/>
