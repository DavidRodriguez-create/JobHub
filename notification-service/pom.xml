<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.davidcreate.jobhub</groupId>
        <artifactId>jobhub-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>notification-service</artifactId>

    <dependencies>
        <!-- Generated JAX-RS interfaces + model classes for this service's contract -->
        <dependency>
            <groupId>com.davidcreate.jobhub</groupId>
            <artifactId>api-contracts</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-client-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-mailer</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-qute</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-scheduler</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-validator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-jwt</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-openapi</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-container-image-jib</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit-mockito</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-test-security</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-test-security-jwt</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.smallrye</groupId>
            <artifactId>smallrye-jwt-build</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.26.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>3.9.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <!-- Three executions keep DevServices to a single shared Postgres container for the
                 vast majority of tests while eliminating two distinct JVM-classloader hazards:

                 1) Plain-Mockito unit tests mock the @RegisterRestClient AppInternalRestClient
                    interface. If they share a fork with @QuarkusTest classes, the Quarkus
                    classloader has already loaded/transformed that interface, and the inline
                    mock maker can no longer redefine it ("Could not modify all classes"). The
                    unit-tests execution gets its own fork group that never boots Quarkus.

                 2) @QuarkusTest classes that declare a non-default @TestProfile force Quarkus to
                    shut down and reboot with a new QuarkusClassLoader. A second such reboot in a
                    fork that already booted Quarkus once collides: the JVM-wide 'app' parent
                    loader still holds the previous QuarkusClassLoader's definition of JAX-RS
                    classes like jakarta.ws.rs.core.Response, so the next QuarkusClassLoader's
                    Class.forName for the same name throws LinkageError ("loader constraint
                    violation"), which then poisons every later test in that fork (Quarkus marks
                    itself doomed and the rest report as skipped). The component-tests execution
                    (default profile only, reuseForks=true) shares ONE fork and therefore ONE
                    DevServices Postgres container for ~20 classes. The component-tests-profiles
                    execution isolates the 4 classes that switch @TestProfile into their own
                    forkCount=1/reuseForks=false group: each gets a fresh JVM (so no stale
                    classloader to collide with) without reintroducing container-per-class churn
                    for the bulk of the suite. -->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Disable the default single execution; replaced by the ones below. -->
                    <skip>true</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>unit-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <skip>false</skip>
                            <reuseForks>true</reuseForks>
                            <forkCount>1</forkCount>
                            <includes>
                                <include>**/unit_tests/**/*Test.java</include>
                            </includes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>component-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <skip>false</skip>
                            <reuseForks>true</reuseForks>
                            <forkCount>1</forkCount>
                            <includes>
                                <include>**/component_tests/**/*Test.java</include>
                            </includes>
                            <excludes>
                                <exclude>**/component_tests/CustomReminderDispatchSchedulerComponentTest.java</exclude>
                                <exclude>**/component_tests/CustomReminderSchedulerKillSwitchComponentTest.java</exclude>
                                <exclude>**/component_tests/GhostedAlertSchedulerKillSwitchComponentTest.java</exclude>
                                <exclude>**/component_tests/WeeklyDigestSchedulerOutageComponentTest.java</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>component-tests-profiles</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <skip>false</skip>
                            <reuseForks>false</reuseForks>
                            <forkCount>1</forkCount>
                            <includes>
                                <include>**/component_tests/CustomReminderDispatchSchedulerComponentTest.java</include>
                                <include>**/component_tests/CustomReminderSchedulerKillSwitchComponentTest.java</include>
                                <include>**/component_tests/GhostedAlertSchedulerKillSwitchComponentTest.java</include>
                                <include>**/component_tests/WeeklyDigestSchedulerOutageComponentTest.java</include>
                            </includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Generate JWT keys at build time (config inherited from parent pluginManagement). -->
            <plugin>
                <groupId>org.codehaus.gmavenplus</groupId>
                <artifactId>gmavenplus-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
